import { postSchema } from './post'
import { categorySchema } from './category'
import { siteSettingsSchema } from './siteSettings'

export const schemaTypes = [postSchema, categorySchema, siteSettingsSchema]
